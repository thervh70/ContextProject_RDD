{
  "name": "octopeer-github",
  "version": "0.1.0",
  "description": "This extension collects data while the user reviews Pull Requests.",
  "scripts": {
    "test": "karma start karma.config.js --single-run --browsers Chrome",
    "tsc": "tsc --project src/octopeer-github/main && tsc --project src/octopeer-github/test"
  },
  "devDependencies": {
    "jasmine": "^2.4.1",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.3",
    "karma-coverage": "^0.5.5",
    "karma-jasmine": "^0.3.8"
  }
}
